Perintah Install (Auto Semua)

Jalankan perintah ini di VPS kamu (Ubuntu/Debian):

```bash
wget https://raw.githubusercontent.com/harunkl/Zizi/main/install.sh -O install.sh && chmod +x install.sh && bash install.sh
